# StreamForge Configuration
# Copy to .env and fill in your values

# ─── Server ───────────────────────────────────────────────
PORT=4242

# ─── vMix Integration ─────────────────────────────────────
# vMix must be running with HTTP API enabled (Settings → Web Controller)
VMIX_HOST=localhost
VMIX_PORT=8088

# Optional: vMix input name to push highlighted comments to
# This is the name of your "Lower Third" or "Social" title input in vMix
# Leave empty to use Data Source XML only (http://localhost:4242/vmix/feed.xml)
VMIX_INPUT=

# Set to false to disable pushing to vMix (monitoring only)
PUSH_TO_VMIX=true

# ─── AI Moderation ────────────────────────────────────────
# Requires Ollama running locally (ollama.ai)
MODERATION=true

# STRICT_MODE=true filters aggressively (churches, family content)
# STRICT_MODE=false filters spam/hate only (general production)
STRICT_MODE=false

# Ollama model for moderation (llama3.2 is fast, qwen2.5:32b is smarter)
AI_MODEL=llama3.2

# ─── YouTube ──────────────────────────────────────────────
# Get API key: https://console.cloud.google.com
YOUTUBE_API_KEY=

# Get Live Chat ID from YouTube Live Dashboard or Studio
# Format: Kicj1234abcd
YOUTUBE_LIVE_CHAT_ID=

# ─── Twitch ───────────────────────────────────────────────
# Channel name (lowercase, e.g. "shroud")
TWITCH_CHANNEL=

# For read-only anonymous access, leave these empty
# For sending messages or moderation, fill in:
TWITCH_USERNAME=
TWITCH_ACCESS_TOKEN=

# ─── TikTok ───────────────────────────────────────────────
# TikTok username (without @)
TIKTOK_USERNAME=

# ─── Feed Settings ────────────────────────────────────────
# Max comments to keep in memory
MAX_FEED_SIZE=50
